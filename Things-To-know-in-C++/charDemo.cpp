#include<iostream>
using namespace std; // allows the use of standard library features without prefixing with std::
int main(){
    // char ch;
    // cin >> ch; // reads a character input from the user and stores it in variable ch
    // cout << ch; // prints the character read from the input
    // return 0; // returns 0 to indicate successful execution of the program
    char ch='g';
    cout << ch;
    return 0;
}